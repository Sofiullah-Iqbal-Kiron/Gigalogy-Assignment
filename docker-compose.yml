version: '3.8'

services:
  web:
    build: .
    command: uvicorn app.main:app --reload --workers 1
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    depends_on:
      - db
  db:
    image: postgres
    volumes:
      - ./db:/var/lib/postgresql/data